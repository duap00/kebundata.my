# --- KEBUN DATA V2 SECURE IGNORE ---

# 1. Private Keys & Security (CRITICAL)
# This prevents your OCI deployment keys from leaking
kebundata*
*.pub
*.pem
id_rsa*
id_ed25519*
secrets.txt

# 2. Environment Variables
# Keeps your Supabase/API keys off the internet
.env
*.env.local
.env.development.local
.env.test.local
.env.production.local

# 3. Dependencies
# Never upload node_modules (they are too big)
node_modules/
jspm_packages/

# 4. Production Builds
# The OCI server builds these fresh, so we don't need to push them
dist/
dist-ssr/
build/
out/

# 5. Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 6. Editor & OS Files
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 7. Testing
/coverage
/.nyc_output

# --- END OF FILE ---